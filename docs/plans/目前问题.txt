问题1：动画生成部分，后处理应该单独处理，目前的逻辑将所有的图片都包在了16*16的图片，实际的逻辑应该是每个单独的图片是16*16，然后拼接成一整个图片。（此外，导出时json确实可以生成，但是内容是否正确这里还没验证、这里可以等后期全部调通之后再处理）
问题2：导出界面的浏览按钮，和选择文件夹按钮，没有实际的功能，浏览按钮一个可以选择本地的文件，然后把选择的文件路径放在导出路径中，而选择文件按钮应该可以打开素材对应的文件夹，方便快速找到导出的素材。